-- Initialize MySQL database
-- This script runs when the MySQL container is first created

-- Create additional databases if needed
-- CREATE DATABASE IF NOT EXISTS live_commerce_test;

-- Grant privileges
-- GRANT ALL PRIVILEGES ON live_commerce.* TO 'livecommerce'@'%';
-- GRANT ALL PRIVILEGES ON live_commerce_test.* TO 'livecommerce'@'%';

-- FLUSH PRIVILEGES;

-- You can add initial table creation here if not using Alembic migrations
